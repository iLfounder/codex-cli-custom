From 3cc1b8f18626d69c1c4bea7e9ce0d2d82fd4b758 Mon Sep 17 00:00:00 2001
From: Codex CLI Custom <codex-cli-custom@users.noreply.github.com>
Date: Fri, 21 Aug 2026 10:14:50 +0900
Subject: [PATCH 08/11] P008: relinquish thread writers strictly

---
 codex-rs/app-server/src/message_processor.rs  |   8 +-
 codex-rs/app-server/tests/suite/v2/mod.rs     |   1 +
 .../tests/suite/v2/thread_relinquish.rs       |  87 ++++++++++++++
 .../core/src/realtime_conversation_tests.rs   |  51 ++++++++
 codex-rs/core/src/session/turn_input.rs       |  21 +++-
 codex-rs/core/src/session/turn_input_tests.rs | 111 ++++++++++++++++++
 codex-rs/core/src/tasks/mod.rs                |   8 ++
 7 files changed, 283 insertions(+), 4 deletions(-)
 create mode 100644 codex-rs/app-server/tests/suite/v2/thread_relinquish.rs

diff --git a/codex-rs/app-server/src/message_processor.rs b/codex-rs/app-server/src/message_processor.rs
index a31b482..68c129c 100644
--- a/codex-rs/app-server/src/message_processor.rs
+++ b/codex-rs/app-server/src/message_processor.rs
@@ -975,9 +975,11 @@ impl MessageProcessor {
             ClientRequest::ThreadAccountSwitch { .. } => Err(method_not_found(
                 "thread/account/switch is not implemented yet",
             )),
-            ClientRequest::ThreadRelinquish { .. } => {
-                Err(method_not_found("thread/relinquish is not implemented yet"))
-            }
+            ClientRequest::ThreadRelinquish { params, .. } => self
+                .session_runtime
+                .relinquish(connection_id, params)
+                .await
+                .map(|response| Some(response.into())),
             ClientRequest::ConfigRead { params, .. } => self
                 .config_processor
                 .read(params)
diff --git a/codex-rs/app-server/tests/suite/v2/mod.rs b/codex-rs/app-server/tests/suite/v2/mod.rs
index 553819b..e2212df 100644
--- a/codex-rs/app-server/tests/suite/v2/mod.rs
+++ b/codex-rs/app-server/tests/suite/v2/mod.rs
@@ -93,6 +93,7 @@ mod thread_metadata_update;
 mod thread_name_websocket;
 mod thread_queue;
 mod thread_read;
+mod thread_relinquish;
 mod thread_resume;
 mod thread_revert;
 mod thread_rollback;
diff --git a/codex-rs/app-server/tests/suite/v2/thread_relinquish.rs b/codex-rs/app-server/tests/suite/v2/thread_relinquish.rs
new file mode 100644
index 0000000..5e7879a
--- /dev/null
+++ b/codex-rs/app-server/tests/suite/v2/thread_relinquish.rs
@@ -0,0 +1,87 @@
+use anyhow::Result;
+use app_test_support::MockResponsesConfig;
+use app_test_support::TestAppServer;
+use app_test_support::create_mock_responses_server_repeating_assistant;
+use codex_app_server_protocol::ClientRequest;
+use codex_app_server_protocol::SessionRuntimeLifecycleState;
+use codex_app_server_protocol::SessionRuntimeListParams;
+use codex_app_server_protocol::SessionRuntimeListResponse;
+use codex_app_server_protocol::SessionRuntimeOperationStatus;
+use codex_app_server_protocol::SessionRuntimeWriterState;
+use codex_app_server_protocol::ThreadRelinquishParams;
+use codex_app_server_protocol::ThreadRelinquishResponse;
+use codex_app_server_protocol::ThreadStartParams;
+use codex_app_server_protocol::ThreadStartResponse;
+use pretty_assertions::assert_eq;
+use tempfile::TempDir;
+
+#[tokio::test]
+async fn idle_thread_relinquish_publishes_terminal_snapshot_and_releases_writer() -> Result<()> {
+    let codex_home = TempDir::new()?;
+    let server = create_mock_responses_server_repeating_assistant("unused").await;
+    MockResponsesConfig::new(&server.uri()).write(codex_home.path())?;
+    let mut app = TestAppServer::builder()
+        .with_codex_home(codex_home.path())
+        .build_initialized()
+        .await?;
+    let ThreadStartResponse { thread, .. } = app
+        .start_thread(ThreadStartParams {
+            model: Some("gpt-5.2".to_string()),
+            ..Default::default()
+        })
+        .await?;
+
+    let before: SessionRuntimeListResponse = app
+        .request(|request_id| ClientRequest::SessionRuntimeList {
+            request_id,
+            params: SessionRuntimeListParams {
+                thread_id: Some(thread.id.clone()),
+                cursor: None,
+                limit: None,
+            },
+        })
+        .await?;
+    let snapshot = before.data.into_iter().next().expect("runtime snapshot");
+    assert_eq!(snapshot.lifecycle.state, SessionRuntimeLifecycleState::Idle);
+    assert_eq!(snapshot.writer.state, SessionRuntimeWriterState::OwnedHere);
+
+    let released: ThreadRelinquishResponse = app
+        .request(|request_id| ClientRequest::ThreadRelinquish {
+            request_id,
+            params: ThreadRelinquishParams {
+                operation_id: "release-idle-thread".to_string(),
+                thread_id: thread.id.clone(),
+                expected_instance_epoch: before.instance_epoch,
+                expected_state_revision: snapshot.state_revision,
+                expected_writer_generation: snapshot
+                    .writer
+                    .writer_generation
+                    .expect("writer generation"),
+            },
+        })
+        .await?;
+    assert_eq!(
+        released.operation.status,
+        SessionRuntimeOperationStatus::Released
+    );
+
+    let after: SessionRuntimeListResponse = app
+        .request(|request_id| ClientRequest::SessionRuntimeList {
+            request_id,
+            params: SessionRuntimeListParams {
+                thread_id: Some(thread.id),
+                cursor: None,
+                limit: None,
+            },
+        })
+        .await?;
+    let snapshot = after.data.into_iter().next().expect("terminal snapshot");
+    assert_eq!(
+        (snapshot.lifecycle.state, snapshot.writer.state),
+        (
+            SessionRuntimeLifecycleState::NotLoaded,
+            SessionRuntimeWriterState::None,
+        )
+    );
+    Ok(())
+}
diff --git a/codex-rs/core/src/realtime_conversation_tests.rs b/codex-rs/core/src/realtime_conversation_tests.rs
index 53b5faa..829ac5a 100644
--- a/codex-rs/core/src/realtime_conversation_tests.rs
+++ b/codex-rs/core/src/realtime_conversation_tests.rs
@@ -3,16 +3,21 @@ use super::HANDOFF_STREAM_TRUNCATION_MARKER;
 use super::RealtimeHandoffState;
 use super::RealtimeSessionKind;
 use super::RealtimeStreamedItem;
+use super::handle_start;
 use super::realtime_delegation_from_handoff;
 use super::realtime_request_headers;
 use super::realtime_text_from_handoff_request;
 use super::wrap_realtime_delegation_input;
 use crate::context::RealtimeDelegationSource;
+use crate::session::tests::make_session_and_context_with_rx;
 use async_channel::bounded;
 use codex_api::RealtimeEventParser;
+use codex_protocol::error::CodexErrorDetails;
 use codex_protocol::models::MessagePhase;
 use codex_protocol::protocol::CodexResponseHandoffMode;
+use codex_protocol::protocol::ConversationStartParams;
 use codex_protocol::protocol::RealtimeHandoffRequested;
+use codex_protocol::protocol::RealtimeOutputModality;
 use codex_protocol::protocol::RealtimeTranscriptEntry;
 use pretty_assertions::assert_eq;
 use std::collections::BTreeMap;
@@ -20,6 +25,52 @@ use std::sync::Arc;
 use std::time::Instant;
 use tokio::sync::Mutex;
 
+#[tokio::test]
+#[expect(
+    clippy::await_holding_invalid_type,
+    reason = "the test holds the transition fence while entering the closing state"
+)]
+async fn realtime_start_rejects_a_closing_execution_runtime() {
+    let (session, _turn_context, _rx) = make_session_and_context_with_rx().await;
+    let transition = session.execution_runtime_transition_lock.lock().await;
+    session
+        .begin_execution_control_transition()
+        .await
+        .expect("idle session should enter the closing state");
+    drop(transition);
+
+    let result = handle_start(
+        &session,
+        "realtime-start".to_string(),
+        ConversationStartParams {
+            client_managed_handoffs: false,
+            delegation_ack_filler: None,
+            flush_transcript_tail_on_session_end: false,
+            codex_responses_as_items: false,
+            codex_response_item_prefix: None,
+            codex_response_handoff_mode: CodexResponseHandoffMode::Thinking,
+            codex_response_handoff_channel_prefixes: None,
+            model: None,
+            output_modality: RealtimeOutputModality::Audio,
+            include_startup_context: false,
+            initial_items: Vec::new(),
+            realtime_start_instructions: None,
+            realtime_end_instructions: None,
+            prompt: None,
+            realtime_session_id: None,
+            transport: None,
+            version: None,
+            voice: None,
+        },
+    )
+    .await;
+
+    assert!(matches!(
+        result.expect_err("closing runtime should reject realtime start").details(),
+        CodexErrorDetails::InvalidRequest(message) if message == "thread runtime is closing"
+    ));
+}
+
 #[test]
 fn prefers_handoff_input_transcript_over_active_transcript() {
     let handoff = RealtimeHandoffRequested {
diff --git a/codex-rs/core/src/session/turn_input.rs b/codex-rs/core/src/session/turn_input.rs
index 90e4560..0db8f2e 100644
--- a/codex-rs/core/src/session/turn_input.rs
+++ b/codex-rs/core/src/session/turn_input.rs
@@ -138,12 +138,22 @@ impl PreparedTurnInputSettings {
     }
 }
 
+#[expect(
+    clippy::await_holding_invalid_type,
+    reason = "turn admission and execution control share one transition fence"
+)]
 pub(super) async fn handle(
     session: &Arc<Session>,
     request: TurnInputRequest,
     mode: TurnInputMode,
     submission_id: String,
 ) -> CodexResult<TurnInputSubmission> {
+    let _transition = session.execution_runtime_transition_lock.lock().await;
+    if session.execution_control_is_closing() {
+        return Err(CodexErr::InvalidRequest(
+            "thread runtime is closing".to_string(),
+        ));
+    }
     match mode {
         TurnInputMode::StartOrSteer => start_or_steer(session, request, submission_id).await,
         TurnInputMode::StartIfIdle => {
@@ -155,11 +165,21 @@ pub(super) async fn handle(
     }
 }
 
+#[expect(
+    clippy::await_holding_invalid_type,
+    reason = "turn recovery admission and execution control share one transition fence"
+)]
 pub(super) async fn handle_recovery(
     session: &Arc<Session>,
     thread_settings: ThreadSettingsOverrides,
     submission_id: String,
 ) -> CodexResult<TurnInputSubmission> {
+    let _transition = session.execution_runtime_transition_lock.lock().await;
+    if session.execution_control_is_closing() {
+        return Err(CodexErr::InvalidRequest(
+            "thread runtime is closing".to_string(),
+        ));
+    }
     let request = TurnInputRequest::user_input(Vec::new()).with_thread_settings(thread_settings);
     start_if_idle(session, request, submission_id, /*is_recovery*/ true).await
 }
@@ -292,7 +312,6 @@ async fn start_if_idle(
 
     if session.input_queue.has_trigger_turn_mailbox_items().await {
         session.clear_reserved_idle_turn(&turn_state).await;
-        session.maybe_start_turn_for_pending_work().await;
         return Ok(TurnInputSubmission::NotSubmitted {
             reason: NotSubmittedReason::PendingTriggerTurn,
         });
diff --git a/codex-rs/core/src/session/turn_input_tests.rs b/codex-rs/core/src/session/turn_input_tests.rs
index b1eb5f0..687eab9 100644
--- a/codex-rs/core/src/session/turn_input_tests.rs
+++ b/codex-rs/core/src/session/turn_input_tests.rs
@@ -64,6 +64,117 @@ fn user_message(text: &str) -> ResponseItem {
     }
 }
 
+#[tokio::test]
+#[expect(
+    clippy::await_holding_invalid_type,
+    reason = "the test proves turn admission waits on the transition fence"
+)]
+async fn turn_input_admission_waits_for_execution_control_transition() {
+    let (session, _turn_context, _rx) = make_session_and_context_with_rx().await;
+    let transition = session.execution_runtime_transition_lock.lock().await;
+    let session_for_submission = Arc::clone(&session);
+    let mut submission = tokio::spawn(async move {
+        handle(
+            &session_for_submission,
+            TurnInputRequest::user_input(vec![UserInput::Text {
+                text: "queued while switching".to_string(),
+                text_elements: Vec::new(),
+            }]),
+            TurnInputMode::Steer {
+                expected_turn_id: "missing-turn".to_string(),
+            },
+            "serialized-submission".to_string(),
+        )
+        .await
+    });
+
+    assert!(
+        tokio::time::timeout(std::time::Duration::from_millis(20), &mut submission)
+            .await
+            .is_err()
+    );
+    drop(transition);
+    assert_eq!(
+        submission
+            .await
+            .expect("submission task should finish")
+            .unwrap(),
+        TurnInputSubmission::NotSubmitted {
+            reason: NotSubmittedReason::NoActiveTurn,
+        }
+    );
+}
+
+#[tokio::test]
+#[expect(
+    clippy::await_holding_invalid_type,
+    reason = "the test proves mailbox admission waits on the transition fence"
+)]
+async fn mailbox_admission_waits_for_execution_control_transition() {
+    let (session, _turn_context, _rx) = make_session_and_context_with_rx().await;
+    let transition = session.execution_runtime_transition_lock.lock().await;
+    let session_for_delivery = Arc::clone(&session);
+    let mut delivery = tokio::spawn(async move {
+        crate::session::handlers::inter_agent_communication(
+            &session_for_delivery,
+            "mailbox-submission".to_string(),
+            InterAgentCommunication::new(
+                AgentPath::root(),
+                AgentPath::root(),
+                Vec::new(),
+                "queued while switching".to_string(),
+                /*trigger_turn*/ false,
+            ),
+            /*parent_turn_id*/ None,
+            /*root_turn_id*/ None,
+        )
+        .await;
+    });
+
+    assert!(
+        tokio::time::timeout(std::time::Duration::from_millis(20), &mut delivery)
+            .await
+            .is_err()
+    );
+    assert!(!session.input_queue.has_pending_mailbox_items().await);
+    drop(transition);
+    delivery.await.expect("mailbox delivery should finish");
+    assert!(session.input_queue.has_pending_mailbox_items().await);
+}
+
+#[tokio::test]
+#[expect(
+    clippy::await_holding_invalid_type,
+    reason = "the test holds the transition fence across the final control recheck"
+)]
+async fn execution_control_commit_rechecks_pending_mailbox_without_closing_thread() {
+    let (session, _turn_context, _rx) = make_session_and_context_with_rx().await;
+    let binding_before = session.execution_account().await.binding.clone();
+    session
+        .input_queue
+        .enqueue_mailbox_communication(
+            InterAgentCommunication::new(
+                AgentPath::root(),
+                AgentPath::root(),
+                Vec::new(),
+                "pending mailbox".to_string(),
+                /*trigger_turn*/ false,
+            ),
+            /*parent_turn_id*/ None,
+            /*root_turn_id*/ None,
+        )
+        .await;
+
+    let transition = session.execution_runtime_transition_lock.lock().await;
+    assert!(matches!(
+        session.begin_execution_control_transition().await,
+        Err(crate::execution_account::ExecutionAccountSwitchError::ThreadBusy)
+    ));
+    assert!(!session.execution_control_is_closing());
+    assert_eq!(session.execution_account().await.binding, binding_before);
+    drop(transition);
+}
+
 async fn submit_start_only(
     session: &Arc<Session>,
     input: SubmittedTurnInput,
diff --git a/codex-rs/core/src/tasks/mod.rs b/codex-rs/core/src/tasks/mod.rs
index cc71acb..0c4f1bb 100644
--- a/codex-rs/core/src/tasks/mod.rs
+++ b/codex-rs/core/src/tasks/mod.rs
@@ -460,10 +460,18 @@ impl Session {
     ///
     /// The turn is created only when the session is idle and mailbox mail either requests a turn
     /// or can wake an outstanding durable sleep.
+    #[expect(
+        clippy::await_holding_invalid_type,
+        reason = "automatic turn admission and execution control share one transition fence"
+    )]
     pub(crate) async fn maybe_start_turn_for_pending_work_with_sub_id(
         self: &Arc<Self>,
         sub_id: String,
     ) {
+        let _transition = self.execution_runtime_transition_lock.lock().await;
+        if self.execution_control_is_closing() {
+            return;
+        }
         if !self.input_queue.has_pending_mailbox_items().await
             || (!self.input_queue.has_trigger_turn_mailbox_items().await
                 && !self.has_outstanding_durable_sleep())
-- 
2.50.1 (Apple Git-155)

